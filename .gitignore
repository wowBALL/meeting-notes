.env
inbox/
failed/
meetings/
state/
__pycache__/
*.pyc
.venv/
venv/
.worktrees/
watcher_*.log

# ทะเบียนน้ำเสียง -- เวกเตอร์เสียงเป็นข้อมูล biometric และ repo นี้เป็น public
speakers/

# Design specs and implementation plans: kept on disk for the workflow that
# writes them, but out of version control so they are not published.
docs/superpowers/

# เสียงพูดดิบของคนจริงที่รอลงทะเบียน -- อ่อนไหวกว่าเวกเตอร์ใน speakers/ ที่ ignore
# ไปแล้วเสียอีก และ repo นี้เป็น public
enroll/

# ชื่อคนจริงกับฝ่ายที่สังกัด และ repo นี้เป็น public -- ดูโครงไฟล์ที่ teams.example.md
teams.md

# ตารางศัพท์ของจริง -- เคย commit ไว้ตอนแรกเพราะคิดว่าเป็นศัพท์เทคนิคล้วน แต่ของจริงมัน
# โตไปทางชื่อโปรเจกต์ ชื่อระบบภายใน และคำย่อของงาน ซึ่งไม่ควรอยู่ใน repo สาธารณะ
# โครงตั้งต้นอยู่ที่ glossary.example.md -- คัดลอกเป็น glossary.md แล้วแก้ได้เลย
glossary.md

# หน้าตาที่ใช้ตกลงดีไซน์กันตอนแรก มันจะ drift จาก web/ ที่เป็นของจริงแน่นอน
# เก็บไว้บนดิสก์ ไม่เข้า repo
runner-mock.html
enroll-mock.html

# config ของเครื่องมือฝั่ง dev ไม่ใช่ของโปรเจกต์ -- เคยหลุดเข้า commit มาแล้วครั้งหนึ่ง
# เพราะ git add -A ในสคริปต์เขียนประวัติ
.claude/

# ของที่ tools/calibrate_voiceprints.py กินและคาย -- ไฟล์เฉลยจับป้าย SPEAKER_xx เข้ากับ
# ชื่อคนจริงตรง ๆ (อ่อนไหวแบบเดียวกับ teams.md) ส่วนอีกสองโฟลเดอร์คือเสียงพูดดิบของคน
# จริงกับผลที่ผูกกับมัน อ่อนไหวแบบเดียวกับ enroll/ -- ดูรูปแบบไฟล์ที่ tools/README.md
speakers-truth.json
.calib-cache/
.calib-clips/
