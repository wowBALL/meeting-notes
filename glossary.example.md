# Glossary -- ไฟล์ตัวอย่าง

คัดลอกเป็น `glossary.md` แล้วแก้ได้ตรงๆ ไม่ต้องแตะโค้ด ไม่ต้องรันเทสต์ใหม่
ประชุมครั้งถัดไปใช้ค่าใหม่เลย

**`glossary.md` ถูก gitignore ไว้แล้ว** เพราะ repo นี้เป็น public และตารางศัพท์ของจริง
มักมีชื่อโปรเจกต์ ชื่อระบบภายใน หรือชื่อลูกค้าปนอยู่ ไฟล์ตัวอย่างนี้จึงเก็บแต่ชื่อ
เทคโนโลยีที่รู้กันทั่วไปไว้เป็นโครงตั้งต้น (ชื่อคนไม่ต้องใส่ที่นี่ —
ดู `teams.md` กับ speaker registry)

ไม่มี `glossary.md` = ระบบทำงานปกติ แค่ไม่กรองคำอะไรเลย ไม่มี error

บรรทัดที่ขึ้นต้นด้วย `#` เป็น comment / `## ชื่อ` คือหัว section

## exact
# แทนที่ตรงๆ ในโค้ด ไม่กิน token เลย
# รูปแบบ: คำถูก: คำผิด1, คำผิด2
#
# กฎเดียวที่ต้องจำ: ภาษาไทยไม่มีช่องว่างระหว่างคำ การแทนที่จึงเป็น substring match
# ล้วน คำผิดสั้นๆ (ต่ำกว่า ~4 อักขระ) จะไปโดนกลางคำอื่น ถ้าไม่มั่นใจให้ใส่ `fuzzy`
#
# สองข้อที่ตามมาจากกฎนั้น ทั้งคู่วัดเจอกับประชุมจริงแล้ว (2026-08-01):
#
# 1. ห้ามใส่คำที่มีความหมายของตัวเองในช่องคำผิด -- มีคนพูดว่า "มันจะ Engage ให้ได้ไหม"
#    ถามว่าเครื่องมือจะจัดไฟล์ให้เองได้ไหม แต่ `Engage` ถูกใส่ไว้เป็นคำผิดของ Ingress
#    ประโยคที่โมเดลได้รับจึงเป็น "มันจะ Ingress ให้ได้ไหม" ซึ่งไม่มีความหมาย และเพราะ
#    exact แก้ก่อนโมเดลเห็น จึงไม่มีใครรู้ได้เลยว่าเดิมพูดว่าอะไร
#
# 2. ห้ามใส่คำผิดที่เป็น "คำนำหน้า" ของคำที่มีความหมาย -- ถอด `Control Panel` ออกจาก
#    คำผิดของ Control Plane อย่างเดียวไม่พอ ถ้ายังเหลือ `Control Pane` ไว้ มันจะกิน
#    "Control Panel" ไปครึ่งเดียวจนได้ "Control Planel" ซึ่งแย่กว่าเดิม
#
# อยากได้ความสามารถแก้คำกลุ่มนี้ ให้ใส่ `fuzzy` พร้อม comment บอกเหตุผล โมเดลจะตัดสิน
# ตามบริบทแทนที่จะแทนที่ทุกครั้งที่เจอ
#
# คำผิดด้านล่างเป็นการ "เดา" ว่า whisper จะถอดเพี้ยนแบบไหน ยังไม่ได้วัดกับเสียงจริง
# ให้ดูบรรทัด "แก้คำตาม glossary" ท้าย summary.md แล้วปรับตามของจริงที่เจอ
PostgreSQL: โพสเกรส, พอสเกรส, โพสต์เกรส
TypeScript: ไทป์สคริปต์, ทัยป์สคริป
JavaScript: จาวาสคริปต์, จาวาสคริป
SvelteKit: สเว็ลคิต, สเวลท์คิต, สเวลคิด
Playwright: เพลย์ไรท์, เพลย์ไรต์
Docker: ด็อกเกอร์, ดอกเกอร์
Redis: เรดิส, เรดีส

## fuzzy
# ให้โมเดลตัดสินตามบริบท ใส่เงื่อนไขต่อท้ายเป็น comment ได้
# กิน token ทุกครั้งที่สรุป -- คำที่ไม่โผล่หลายสัปดาห์ให้ลบออก
# (ดูบรรทัด "คำ fuzzy ที่เจอในห้อง" ท้าย summary.md ว่าคำไหนยังมีชีวิต)
Electron: อิเล็กตรอน  # เฉพาะตอนคุยเรื่อง desktop app ไม่ใช่บริบทฟิสิกส์
Tesseract: เทสเซอแร็ค, เทสแซค

## project-names
# ชื่อโปรเจกต์ภายใน คงรูปตามนี้เสมอ
# ว่างไว้โดยเจตนา -- ใส่แล้วอย่าลืมเรื่อง public repo ด้านบน
# รูปแบบ: ชื่อจริง: ที่ถอดเพี้ยน1, ที่ถอดเพี้ยน2

## aliases
# เรื่องเดียวกัน คนละฝ่ายเรียกคนละชื่อ -- ยุบเป็นชื่อกลางก่อนสรุป
# ไม่ใช่การแก้คำผิด ทั้งสองคำถูกทั้งคู่ แต่ถ้าไม่ยุบ map-reduce จะแยกเป็น 2 หัวข้อ
# รูปแบบ: ชื่อกลาง: ที่ Business เรียก, ที่ dev เรียก
#
# ก่อนเพิ่มให้ถามตัวเองว่า "ถ้าลูกค้าถามถึงอันนี้ ทั้งสองฝ่ายจะเปิดหน้าจอเดียวกันไหม"
# ถ้าไม่ใช่ แปลว่ามันคนละของจริงๆ การยุบจะทำให้สรุปผิด
# ตัวอย่างรูปแบบ (comment ไว้ เพราะเป็นโครงระบบภายใน):
# ระบบชำระเงิน: หน้าจ่ายเงิน, checkout, payment flow
# ระบบสมาชิก: หน้าสมัคร, auth, ระบบล็อกอิน

## ambiguous
# คำเดียวกัน คนละฝ่ายหมายคนละเรื่อง -- ให้โมเดลแยกตามฝ่ายของผู้พูด
# เข้า prompt เฉพาะประชุมข้ามฝ่าย และต้องมี teams.md คู่กันไม่งั้นแทบไม่ช่วยอะไร
# รูปแบบ: คำ | Business = ... | dev = ...
เสร็จ | Business = เอาไป demo ให้ลูกค้าได้ | dev = code merge แล้ว ยังไม่ deploy
ระบบ | Business = ตัวสินค้าที่ลูกค้าเห็น | dev = service ตัวใดตัวหนึ่ง
ลูกค้า | Business = บริษัทที่ซื้อ | dev = end user ที่กดใช้งาน
เคส | Business = เคสลูกค้า/ดีล | dev = test case หรือ edge case
ไปป์ไลน์ | Business = งาน/ดีลที่กำลังตามอยู่ | dev = CI/CD หรือ data pipeline
