# ฝ่ายของผู้เข้าร่วม -- ไฟล์ตัวอย่าง

คัดลอกเป็น `teams.md` แล้วใส่ชื่อจริง (`teams.md` ถูก gitignore ไว้แล้ว
เพราะมีชื่อคนจริง และ repo นี้เป็น public — ต่างจาก `glossary.md` ที่ commit ได้)

ใช้ให้โมเดลตีความคำในหมวด `ambiguous` ตามฝ่ายของผู้พูด และระบุเจ้าของงานข้ามฝ่าย
เข้า prompt เฉพาะประชุมข้ามฝ่ายเท่านั้น

ชื่อต้องตรงกับที่ตั้งไว้ใน speaker registry (`speakers/registry.json`)
ไม่งั้นโมเดลจับคู่ชื่อใน transcript กับฝ่ายไม่ได้

บรรทัดที่ขึ้นต้นด้วย `#` เป็น comment
รูปแบบ: `ฝ่าย: ชื่อ1, ชื่อ2`

Business: ชื่อคนฝ่าย Business
dev: ชื่อคนฝ่าย dev
